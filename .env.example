# Configuration de la base de données PostgreSQL (Docker)
# Copier ce fichier vers .env et remplir avec vos vraies valeurs
# Note: Le port 5433 correspond au mapping Docker (5433:5432)

DB_HOST=localhost
DB_PORT=5433
DB_NAME=bibliothequeuniv
DB_USER=postgres
DB_PASSWORD=postgres
